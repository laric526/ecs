export function System(name, query, callback) {
    this.name = name;
    this.query = query;
    this.callback = callback;
}
export function System(name, query, callback) {
    this.name = name;
    this.query = query;
    this.callback = callback;
}